openapi: 3.0.3
info:
  title: Single Output API
  version: 0.1.0
paths:
  /users/{id}:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
